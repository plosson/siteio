# Admin UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a browser-based admin UI at `https://api.<domain>/ui` for a siteio operator to inspect apps/sites/groups and run lifecycle actions on apps (deploy/stop/restart/remove) + undeploy/rollback on sites, authenticating with the same API key the CLI uses.

**Architecture:** A pure client-side SPA. Static HTML/CSS/JS lives in `src/lib/agent/ui/`, imported as text (`with { type: "text" }`) so Bun's `--compile` bakes it into the single binary. Three routes (`/ui`, `/ui/app.js`, `/ui/app.css`) are mounted on the existing `Bun.serve` instance via its native `routes` map; the existing `handleRequest` stays untouched as the `fetch` fallback. The UI calls the existing JSON API with `X-API-Key` from `sessionStorage`. No new backend endpoints, no server-side rendering, no HTMX, no Hono, no build step for the frontend.

**Tech Stack:** Bun 1.3+ / TypeScript / `Bun.serve` routes map / Alpine.js 3.x (CDN) / Tailwind CSS (CDN) / HugeIcons (CDN) / Geist + Fira Mono (Google Fonts) / `@playwright/test` for e2e / `bun:test` for integration.

**Reference spec:** `docs/superpowers/specs/2026-04-20-admin-ui-design.md`

---

## Task 1: Shell — routes, static assets, Playwright scaffold

**Goal:** Serve an empty HTML shell at `/ui` with the sidebar rendered. Wire `Bun.serve` routes map to three static assets. Stand up the Playwright scaffold so every later task can add e2e tests.

**Files:**
- Create: `src/lib/agent/ui/index.html`
- Create: `src/lib/agent/ui/app.js`
- Create: `src/lib/agent/ui/app.css`
- Create: `src/lib/agent/ui/assets.ts`
- Modify: `src/lib/agent/server.ts` (start method — switch to routes map)
- Create: `src/__tests__/api/admin-ui-routes.test.ts`
- Create: `tests/playwright/helpers/server.ts`
- Create: `tests/playwright/shell.spec.ts`
- Create: `playwright.config.ts`
- Modify: `package.json` (add `@playwright/test` dev dep + `test:e2e` script)
- Modify: `.gitignore` (add `test-results/`, `playwright-report/`, `.playwright/`)

---

- [ ] **Step 1: Install Playwright**

```bash
bun add -d @playwright/test
bunx playwright install chromium
```

Expected: `@playwright/test` appears in `package.json` devDependencies; Chromium downloaded.

- [ ] **Step 2: Create Playwright config**

Create `playwright.config.ts`:

```ts
import { defineConfig, devices } from "@playwright/test"

export default defineConfig({
  testDir: "./tests/playwright",
  timeout: 30_000,
  expect: { timeout: 5_000 },
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: 0,
  workers: 1,
  reporter: [["list"]],
  use: {
    trace: "on-first-retry",
    screenshot: "only-on-failure",
    video: "retain-on-failure",
  },
  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
  ],
})
```

- [ ] **Step 3: Add test:e2e script + update .gitignore**

Edit `package.json`, add to the `scripts` block:

```json
    "test:e2e": "playwright test"
```

Edit `.gitignore`, append:

```
test-results/
playwright-report/
.playwright/
```

- [ ] **Step 4: Create Playwright server helper**

Create `tests/playwright/helpers/server.ts`:

```ts
import { mkdtempSync, rmSync } from "fs"
import { tmpdir } from "os"
import { join } from "path"
import { AgentServer } from "../../../src/lib/agent/server"
import type { AgentConfig } from "../../../src/types"

export interface TestServerHandle {
  url: string
  apiKey: string
  dataDir: string
  server: AgentServer
  cleanup: () => void
}

let nextPort = 4600

export async function startTestServer(opts: { apiKey?: string } = {}): Promise<TestServerHandle> {
  const apiKey = opts.apiKey ?? "test-api-key"
  const dataDir = mkdtempSync(join(tmpdir(), "siteio-ui-e2e-"))
  const port = nextPort++

  const config: AgentConfig = {
    domain: "test.example.com",
    apiKey,
    dataDir,
    port,
    skipTraefik: true,
    maxUploadSize: 50 * 1024 * 1024,
    httpPort: 80,
    httpsPort: 443,
  }

  const server = new AgentServer(config)
  await server.start()

  return {
    url: `http://127.0.0.1:${port}`,
    apiKey,
    dataDir,
    server,
    cleanup: () => {
      server.stop()
      try { rmSync(dataDir, { recursive: true, force: true }) } catch { /* ignore */ }
    },
  }
}
```

- [ ] **Step 5: Create `index.html` shell**

Create `src/lib/agent/ui/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>siteio admin</title>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700&family=Fira+Mono:wght@400;500&display=swap" rel="stylesheet" />

  <link rel="stylesheet" href="https://sets.hugeicons.com/7v2rm862ty6.css" crossorigin="anonymous" />

  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            brand: {
              blue: "#0969da",
              green: "#2da44e",
            },
            gray: {
              50: "#f6f8fa",
              100: "#eaeef2",
              200: "#d0d7de",
              300: "#afb8c1",
              400: "#8c959f",
              500: "#6e7781",
              600: "#57606a",
              700: "#424a53",
              800: "#32383f",
              900: "#1f2328",
            },
          },
          fontFamily: {
            sans: ["Geist", "system-ui", "sans-serif"],
            mono: ["Fira Mono", "monospace"],
          },
        },
      },
    }
  </script>

  <link rel="stylesheet" href="/ui/app.css" />

  <script defer src="https://unpkg.com/alpinejs@3.15.3/dist/cdn.min.js"></script>
  <script defer src="/ui/app.js"></script>
</head>
<body class="font-sans bg-gray-50 text-gray-900 antialiased" x-data="siteioAdmin()" x-init="init()">

  <template x-if="!authed">
    <div id="login-view" class="min-h-screen flex items-center justify-center p-6">
      <div class="text-gray-500 text-sm">Login view will render here (Task 2).</div>
    </div>
  </template>

  <template x-if="authed">
    <div class="flex min-h-screen">
      <aside class="w-56 shrink-0 border-r border-gray-200 bg-white flex flex-col">
        <div class="p-4 border-b border-gray-200">
          <div class="font-semibold text-gray-900">siteio</div>
          <div class="text-xs text-gray-500 mt-1 font-mono" x-text="hostname"></div>
        </div>
        <nav class="flex-1 p-2 space-y-1">
          <a href="#/apps" :class="navClass('apps')" class="flex items-center gap-2 px-3 py-2 rounded text-sm">
            <i class="hgi hgi-stroke hgi-apps"></i> Apps
          </a>
          <a href="#/sites" :class="navClass('sites')" class="flex items-center gap-2 px-3 py-2 rounded text-sm">
            <i class="hgi hgi-stroke hgi-globe"></i> Sites
          </a>
          <a href="#/groups" :class="navClass('groups')" class="flex items-center gap-2 px-3 py-2 rounded text-sm">
            <i class="hgi hgi-stroke hgi-user-group"></i> Groups
          </a>
        </nav>
        <div class="p-2 border-t border-gray-200">
          <button @click="logout()" class="w-full flex items-center gap-2 px-3 py-2 rounded text-sm text-gray-700 hover:bg-gray-50">
            <i class="hgi hgi-stroke hgi-logout-03"></i> Logout
          </button>
        </div>
      </aside>

      <main class="flex-1 overflow-auto">
        <div class="p-6" id="main-content">
          <div class="text-sm text-gray-500">Main content will render here (later tasks).</div>
        </div>
      </main>
    </div>
  </template>
</body>
</html>
```

- [ ] **Step 6: Create `app.css`**

Create `src/lib/agent/ui/app.css`:

```css
.nav-link {
  color: #424a53;
}
.nav-link:hover {
  background-color: #f6f8fa;
}
.nav-link-active {
  background-color: #f6f8fa;
  color: #0969da;
  font-weight: 500;
  border-left: 2px solid #0969da;
  padding-left: 10px;
}

pre.logs {
  font-family: "Fira Mono", monospace;
  font-size: 12px;
  line-height: 1.5;
  background: #1f2328;
  color: #eaeef2;
  padding: 1rem;
  border-radius: 6px;
  overflow: auto;
  max-height: 70vh;
  white-space: pre-wrap;
  word-break: break-all;
}
```

- [ ] **Step 7: Create `app.js` with minimal Alpine root**

Create `src/lib/agent/ui/app.js`:

```js
// siteio admin UI — single root Alpine component

function siteioAdmin() {
  return {
    // auth
    apiKey: null,
    authed: false,
    loginError: "",

    // route
    route: { view: "apps", param: null, subtab: null },

    // data
    sites: null, apps: null, groups: null,
    selectedSite: null, selectedApp: null,

    // ui
    toasts: [],
    pending: new Set(),
    hostname: "",

    init() {
      this.hostname = window.location.hostname
      const key = sessionStorage.getItem("siteio_api_key")
      if (key) {
        this.apiKey = key
        this.authed = true
      }
      this.parseHash()
      window.addEventListener("hashchange", () => this.parseHash())
      window.addEventListener("siteio:unauthenticated", () => this.onUnauthenticated())
    },

    parseHash() {
      const h = window.location.hash.replace(/^#/, "") || "/apps"
      const parts = h.split("/").filter(Boolean) // ["apps"] or ["apps", "myapp"]
      const view = parts[0] || "apps"
      const param = parts[1] || null
      this.route = { view, param, subtab: null }
    },

    navClass(view) {
      return this.route.view === view ? "nav-link nav-link-active" : "nav-link"
    },

    onUnauthenticated() {
      this.apiKey = null
      this.authed = false
      this.loginError = "Session expired. Please sign in again."
    },

    logout() {
      sessionStorage.removeItem("siteio_api_key")
      this.apiKey = null
      this.authed = false
      this.loginError = ""
    },

    async apiFetch(path, options = {}) {
      const res = await fetch(path, {
        ...options,
        headers: { ...(options.headers || {}), "X-API-Key": this.apiKey },
      })
      if (res.status === 401) {
        sessionStorage.removeItem("siteio_api_key")
        window.dispatchEvent(new CustomEvent("siteio:unauthenticated"))
        throw new Error("Unauthenticated")
      }
      return res
    },
  }
}
```

- [ ] **Step 8: Create `assets.ts`**

Create `src/lib/agent/ui/assets.ts`:

```ts
import indexHtml from "./index.html" with { type: "text" }
import appJs from "./app.js" with { type: "text" }
import appCss from "./app.css" with { type: "text" }

export const ADMIN_UI_HTML = indexHtml
export const ADMIN_UI_JS = appJs
export const ADMIN_UI_CSS = appCss
```

- [ ] **Step 9: Wire routes map into `AgentServer.start()`**

Open `src/lib/agent/server.ts`. Add the import near the top (alongside other lib imports):

```ts
import { ADMIN_UI_HTML, ADMIN_UI_JS, ADMIN_UI_CSS } from "./ui/assets.ts"
```

Replace the `Bun.serve` block inside `start()` (currently lines ~1522–1525) with:

```ts
    // Start HTTP server
    this.server = Bun.serve({
      port,
      routes: {
        "/ui": () =>
          new Response(ADMIN_UI_HTML, {
            headers: { "Content-Type": "text/html; charset=utf-8" },
          }),
        "/ui/app.js": () =>
          new Response(ADMIN_UI_JS, {
            headers: { "Content-Type": "application/javascript; charset=utf-8" },
          }),
        "/ui/app.css": () =>
          new Response(ADMIN_UI_CSS, {
            headers: { "Content-Type": "text/css; charset=utf-8" },
          }),
      },
      fetch: (req) => this.handleRequest(req),
    })
```

Keep the three `console.log` lines after it unchanged.

- [ ] **Step 10: Write failing integration test for routes**

Create `src/__tests__/api/admin-ui-routes.test.ts`:

```ts
import { describe, test, expect, beforeAll, afterAll } from "bun:test"
import { mkdtempSync, rmSync, existsSync } from "fs"
import { join } from "path"
import { tmpdir } from "os"
import { AgentServer } from "../../lib/agent/server"
import type { AgentConfig } from "../../types"

describe("API: Admin UI routes", () => {
  let testDir: string
  let server: AgentServer
  let baseUrl: string
  const apiKey = "test-api-key"
  const testPort = 4701

  beforeAll(async () => {
    testDir = mkdtempSync(join(tmpdir(), "siteio-admin-ui-test-"))
    const config: AgentConfig = {
      domain: "test.example.com",
      apiKey,
      dataDir: testDir,
      port: testPort,
      skipTraefik: true,
      maxUploadSize: 50 * 1024 * 1024,
      httpPort: 80,
      httpsPort: 443,
    }
    server = new AgentServer(config)
    await server.start()
    baseUrl = `http://localhost:${testPort}`
  })

  afterAll(() => {
    server.stop()
    if (existsSync(testDir)) rmSync(testDir, { recursive: true, force: true })
  })

  test("GET /ui returns HTML shell", async () => {
    const res = await fetch(`${baseUrl}/ui`)
    expect(res.status).toBe(200)
    expect(res.headers.get("content-type")).toContain("text/html")
    const body = await res.text()
    expect(body).toContain("<html")
    expect(body).toContain("siteioAdmin()")
  })

  test("GET /ui/app.js returns JS", async () => {
    const res = await fetch(`${baseUrl}/ui/app.js`)
    expect(res.status).toBe(200)
    expect(res.headers.get("content-type")).toContain("application/javascript")
    const body = await res.text()
    expect(body).toContain("function siteioAdmin")
  })

  test("GET /ui/app.css returns CSS", async () => {
    const res = await fetch(`${baseUrl}/ui/app.css`)
    expect(res.status).toBe(200)
    expect(res.headers.get("content-type")).toContain("text/css")
  })

  test("unknown /ui/* path falls through to handleRequest 404", async () => {
    const res = await fetch(`${baseUrl}/ui/nonexistent`)
    expect(res.status).toBe(404)
  })

  test("existing /health endpoint still works (no regression)", async () => {
    const res = await fetch(`${baseUrl}/health`)
    expect(res.status).toBe(200)
    const body = (await res.json()) as { success: boolean; data: { status: string } }
    expect(body.success).toBe(true)
    expect(body.data.status).toBe("ok")
  })

  test("existing /sites endpoint still rejects unauth (no regression)", async () => {
    const res = await fetch(`${baseUrl}/sites`)
    expect(res.status).toBe(401)
  })

  test("existing /sites endpoint still authenticates (no regression)", async () => {
    const res = await fetch(`${baseUrl}/sites`, { headers: { "X-API-Key": apiKey } })
    expect(res.status).toBe(200)
  })
})
```

- [ ] **Step 11: Run the integration tests, verify all pass**

Run: `bun test src/__tests__/api/admin-ui-routes.test.ts`
Expected: 7 pass.

- [ ] **Step 12: Write Playwright shell smoke test**

Create `tests/playwright/shell.spec.ts`:

```ts
import { test, expect } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => { srv = await startTestServer() })
test.afterAll(() => srv.cleanup())

test("GET /ui renders the HTML shell", async ({ page }) => {
  await page.goto(`${srv.url}/ui`)
  // At boot with no session key, authed=false so login placeholder shows
  await expect(page.locator("#login-view")).toBeVisible()
  // Tailwind/Alpine loaded (body has x-data attribute applied; fonts loaded via link preconnect)
  await expect(page.locator("body")).toHaveAttribute("x-data", /siteioAdmin/)
})

test("with a session-storage key, sidebar renders", async ({ page, context }) => {
  // Pre-seed sessionStorage via an init script that runs before Alpine init.
  await context.addInitScript(() => {
    sessionStorage.setItem("siteio_api_key", "test-api-key")
  })
  await page.goto(`${srv.url}/ui`)
  await expect(page.locator("aside")).toBeVisible()
  await expect(page.locator('aside a[href="#/apps"]')).toBeVisible()
  await expect(page.locator('aside a[href="#/sites"]')).toBeVisible()
  await expect(page.locator('aside a[href="#/groups"]')).toBeVisible()
  await expect(page.locator("aside button", { hasText: "Logout" })).toBeVisible()
})
```

- [ ] **Step 13: Run Playwright tests, verify they pass**

Run: `bun run test:e2e`
Expected: 2 pass.

- [ ] **Step 14: Type check**

Run: `bun run typecheck`
Expected: exits 0 with no errors.

- [ ] **Step 15: Commit**

```bash
git add src/lib/agent/ui/ src/lib/agent/server.ts src/__tests__/api/admin-ui-routes.test.ts \
        tests/playwright/ playwright.config.ts package.json bun.lock .gitignore
git commit -m "$(cat <<'EOF'
feat(admin-ui): scaffold /ui route + static assets + Playwright infra

Task 1 of the admin UI plan: empty HTML shell served from the existing
Bun.serve via its routes map, text-imported asset constants, Playwright
scaffold with a server helper, and integration tests confirming no
regression on the existing JSON API.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Login + apiFetch session handling

**Goal:** Render the login form when not authenticated, validate the API key against `/sites`, persist it in `sessionStorage`, centralize 401 handling. Logout clears the session. Session expiry during a fetch flips the UI back to login with a helpful message.

**Files:**
- Modify: `src/lib/agent/ui/index.html` (login view template)
- Modify: `src/lib/agent/ui/app.js` (login method, logout refined)
- Create: `tests/playwright/login.spec.ts`

---

- [ ] **Step 1: Add the real login view to `index.html`**

Replace the placeholder login `<template x-if="!authed">` block with:

```html
  <template x-if="!authed">
    <div id="login-view" class="min-h-screen flex items-center justify-center p-6 bg-gray-50">
      <div class="w-full max-w-sm bg-white rounded-lg shadow p-6">
        <div class="mb-4">
          <h1 class="text-lg font-semibold text-gray-900">Sign in to siteio</h1>
          <p class="text-sm text-gray-500 mt-1">Enter your server API key.</p>
        </div>
        <form @submit.prevent="login()">
          <label class="block text-sm font-medium text-gray-700 mb-1" for="apiKeyInput">API key</label>
          <input
            id="apiKeyInput"
            type="password"
            x-model="apiKeyInput"
            class="w-full px-3 py-2 border border-gray-200 rounded text-sm focus:outline-none focus:ring-2 focus:ring-brand-blue/40 focus:border-brand-blue"
            autocomplete="off"
            required
          />
          <template x-if="loginError">
            <p id="login-error" class="mt-2 text-sm text-red-600" x-text="loginError"></p>
          </template>
          <button
            type="submit"
            :disabled="loginPending"
            class="mt-4 w-full bg-brand-blue hover:bg-brand-blue/90 disabled:opacity-60 text-white font-medium py-2 rounded text-sm"
          >
            <span x-show="!loginPending">Sign in</span>
            <span x-show="loginPending">Signing in…</span>
          </button>
        </form>
      </div>
    </div>
  </template>
```

- [ ] **Step 2: Extend Alpine root with login state and method**

Open `src/lib/agent/ui/app.js`. Add `apiKeyInput: ""` and `loginPending: false` to the returned object (alongside `loginError`). Replace the existing `onUnauthenticated` + `logout` and add a `login` method. The relevant block becomes:

```js
    // auth
    apiKey: null,
    authed: false,
    apiKeyInput: "",
    loginError: "",
    loginPending: false,

    // ... (route, data, ui stay unchanged) ...

    async login() {
      this.loginError = ""
      const candidate = this.apiKeyInput.trim()
      if (!candidate) {
        this.loginError = "API key is required."
        return
      }
      this.loginPending = true
      try {
        const res = await fetch("/sites", {
          headers: { "X-API-Key": candidate },
        })
        if (res.status === 401) {
          this.loginError = "Invalid API key."
          return
        }
        if (!res.ok) {
          this.loginError = `Server returned ${res.status}.`
          return
        }
        sessionStorage.setItem("siteio_api_key", candidate)
        this.apiKey = candidate
        this.authed = true
        this.apiKeyInput = ""
      } catch {
        this.loginError = "Could not reach server."
      } finally {
        this.loginPending = false
      }
    },

    onUnauthenticated() {
      this.apiKey = null
      this.authed = false
      this.loginError = "Session expired. Please sign in again."
    },

    logout() {
      sessionStorage.removeItem("siteio_api_key")
      this.apiKey = null
      this.authed = false
      this.loginError = ""
      this.apiKeyInput = ""
    },
```

- [ ] **Step 3: Write Playwright tests for login/logout/session**

Create `tests/playwright/login.spec.ts`:

```ts
import { test, expect } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => { srv = await startTestServer({ apiKey: "right-key" }) })
test.afterAll(() => srv.cleanup())

test("invalid API key shows inline error", async ({ page }) => {
  await page.goto(`${srv.url}/ui`)
  await page.fill("#apiKeyInput", "wrong-key")
  await page.click('button[type="submit"]')
  await expect(page.locator("#login-error")).toContainText("Invalid API key")
  // still on login view
  await expect(page.locator("#login-view")).toBeVisible()
})

test("valid API key signs in and shows dashboard", async ({ page }) => {
  await page.goto(`${srv.url}/ui`)
  await page.fill("#apiKeyInput", "right-key")
  await page.click('button[type="submit"]')
  await expect(page.locator("aside")).toBeVisible()
  const stored = await page.evaluate(() => sessionStorage.getItem("siteio_api_key"))
  expect(stored).toBe("right-key")
})

test("logout clears session and returns to login", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui`)
  await expect(page.locator("aside")).toBeVisible()
  await page.click('aside button:has-text("Logout")')
  await expect(page.locator("#login-view")).toBeVisible()
  const stored = await page.evaluate(() => sessionStorage.getItem("siteio_api_key"))
  expect(stored).toBeNull()
})

test("401 on any authed fetch triggers session expiry", async ({ page, context }) => {
  // Seed a BAD key so any real fetch will 401
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "stale-key"))
  await page.goto(`${srv.url}/ui`)
  // sidebar renders (authed flipped true at boot based on sessionStorage presence)
  await expect(page.locator("aside")).toBeVisible()
  // Manually trigger apiFetch which will 401 → flip to login with message
  await page.evaluate(async () => {
    // @ts-expect-error access Alpine root for test-only action
    const root = Alpine.$data(document.body)
    try { await root.apiFetch("/sites") } catch { /* expected */ }
  })
  await expect(page.locator("#login-view")).toBeVisible()
  await expect(page.locator("#login-error")).toContainText("Session expired")
})
```

- [ ] **Step 4: Run Playwright tests**

Run: `bun run test:e2e tests/playwright/login.spec.ts`
Expected: 4 pass.

- [ ] **Step 5: Run full test suite**

Run: `bun test && bun run typecheck && bun run test:e2e`
Expected: all pass.

- [ ] **Step 6: Commit**

```bash
git add src/lib/agent/ui/index.html src/lib/agent/ui/app.js tests/playwright/login.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): login form, session handling, centralized 401

Task 2: real login view validates the API key against GET /sites,
persists to sessionStorage, logout clears it, and 401 from any
apiFetch flips the UI back to the login view with an expired message.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Apps list view (read-only)

**Goal:** After login, `#/apps` shows a table of apps fetched from `GET /apps`. Manual refresh button. Row click navigates to `#/apps/:name` (detail stub for now).

**Files:**
- Modify: `src/lib/agent/ui/index.html` (apps list view inside main area)
- Modify: `src/lib/agent/ui/app.js` (loadApps, route-driven fetching)
- Create: `tests/playwright/apps-list.spec.ts`

---

- [ ] **Step 1: Add view rendering scaffolding to main area**

Open `src/lib/agent/ui/index.html`. Replace the `<main>` block's `<div class="p-6" id="main-content">` content with:

```html
        <div class="p-6" id="main-content">
          <!-- Apps list -->
          <template x-if="route.view === 'apps' && !route.param">
            <section>
              <div class="flex items-center justify-between mb-4">
                <h1 class="text-xl font-semibold">Apps</h1>
                <button @click="loadApps()" :disabled="pending.has('apps-list')"
                        class="inline-flex items-center gap-1 px-3 py-1.5 text-sm border border-gray-200 rounded hover:bg-gray-50 disabled:opacity-50">
                  <i class="hgi hgi-stroke hgi-refresh"></i>
                  <span x-show="!pending.has('apps-list')">Refresh</span>
                  <span x-show="pending.has('apps-list')">Loading…</span>
                </button>
              </div>

              <template x-if="apps === null">
                <div class="text-sm text-gray-500">Loading apps…</div>
              </template>

              <template x-if="apps && apps.length === 0">
                <div class="text-sm text-gray-500">No apps yet. Create one with <code class="font-mono">siteio apps create</code>.</div>
              </template>

              <template x-if="apps && apps.length > 0">
                <div class="border border-gray-200 rounded overflow-hidden bg-white">
                  <table class="w-full text-sm">
                    <thead class="bg-gray-50 text-left text-xs uppercase text-gray-600">
                      <tr>
                        <th class="px-3 py-2">Name</th>
                        <th class="px-3 py-2">Source</th>
                        <th class="px-3 py-2">Status</th>
                        <th class="px-3 py-2">Domains</th>
                      </tr>
                    </thead>
                    <tbody>
                      <template x-for="app in apps" :key="app.name">
                        <tr class="border-t border-gray-100 hover:bg-gray-50 cursor-pointer"
                            @click="window.location.hash = '#/apps/' + app.name"
                            :data-app-name="app.name">
                          <td class="px-3 py-2 font-medium" x-text="app.name"></td>
                          <td class="px-3 py-2 text-gray-700" x-text="appSourceLabel(app)"></td>
                          <td class="px-3 py-2">
                            <span :class="statusBadgeClass(app.status)"
                                  class="inline-block px-2 py-0.5 text-xs rounded font-medium"
                                  x-text="app.status"></span>
                          </td>
                          <td class="px-3 py-2 text-gray-700 font-mono text-xs" x-text="(app.domains || []).join(', ') || '—'"></td>
                        </tr>
                      </template>
                    </tbody>
                  </table>
                </div>
              </template>
            </section>
          </template>

          <!-- App detail (stub — filled in Task 4) -->
          <template x-if="route.view === 'apps' && route.param">
            <section>
              <div class="text-sm text-gray-500">App detail for <code class="font-mono" x-text="route.param"></code> — coming in Task 4.</div>
            </section>
          </template>

          <!-- Sites placeholder -->
          <template x-if="route.view === 'sites'">
            <div class="text-sm text-gray-500">Sites coming in Task 7.</div>
          </template>

          <!-- Groups placeholder -->
          <template x-if="route.view === 'groups'">
            <div class="text-sm text-gray-500">Groups coming in Task 8.</div>
          </template>
        </div>
```

- [ ] **Step 2: Extend `app.js` with `loadApps`, route-driven fetching, and display helpers**

Open `src/lib/agent/ui/app.js`. At the end of the returned object (before the closing `}`), add:

```js
    // --- Apps ---

    async loadApps() {
      this.pending.add("apps-list")
      try {
        const res = await this.apiFetch("/apps")
        const body = await res.json()
        if (body.success) {
          this.apps = body.data
        } else {
          this.apps = []
          this.toast("error", body.error || "Failed to load apps")
        }
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.apps = []
          this.toast("error", "Could not reach server")
        }
      } finally {
        this.pending.delete("apps-list")
      }
    },

    appSourceLabel(app) {
      if (app.compose) return "compose"
      if (app.git) return "git"
      if (app.dockerfile) return "dockerfile"
      return "image"
    },

    statusBadgeClass(status) {
      switch (status) {
        case "running": return "bg-green-100 text-green-800"
        case "stopped": return "bg-gray-100 text-gray-700"
        case "failed":  return "bg-red-100 text-red-800"
        case "pending":
        default:        return "bg-amber-100 text-amber-800"
      }
    },

    toast(type, message) {
      const id = Date.now() + Math.random()
      this.toasts.push({ id, type, message })
      setTimeout(() => { this.toasts = this.toasts.filter(t => t.id !== id) }, 4000)
    },
```

Then update `parseHash()` so it triggers data loads when the relevant view is entered. Replace the current `parseHash` with:

```js
    parseHash() {
      const h = window.location.hash.replace(/^#/, "") || "/apps"
      const parts = h.split("/").filter(Boolean)
      const view = parts[0] || "apps"
      const param = parts[1] || null
      this.route = { view, param, subtab: null }
      if (this.authed) this.onRouteEnter()
    },

    onRouteEnter() {
      if (this.route.view === "apps" && !this.route.param) this.loadApps()
      // detail + sites + groups wired in later tasks
    },
```

Also update `init()` — after setting `authed`, call `onRouteEnter()` once so the initial view fetches. Replace `init()`:

```js
    init() {
      this.hostname = window.location.hostname
      const key = sessionStorage.getItem("siteio_api_key")
      if (key) {
        this.apiKey = key
        this.authed = true
      }
      this.parseHash()
      window.addEventListener("hashchange", () => this.parseHash())
      window.addEventListener("siteio:unauthenticated", () => this.onUnauthenticated())
      if (this.authed) this.onRouteEnter()
    },
```

And update `login()` — after successful sign-in, trigger the initial load. In the success branch (after setting `this.authed = true`), add:

```js
        this.onRouteEnter()
```

- [ ] **Step 3: Write Playwright test — apps list**

Create `tests/playwright/apps-list.spec.ts`:

```ts
import { test, expect } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => {
  srv = await startTestServer({ apiKey: "right-key" })
  // Seed two apps directly through the API (same path the CLI uses).
  await fetch(`${srv.url}/apps`, {
    method: "POST",
    headers: { "X-API-Key": srv.apiKey, "Content-Type": "application/json" },
    body: JSON.stringify({ name: "app-one", image: "nginx:alpine", internalPort: 80 }),
  })
  await fetch(`${srv.url}/apps`, {
    method: "POST",
    headers: { "X-API-Key": srv.apiKey, "Content-Type": "application/json" },
    body: JSON.stringify({ name: "app-two", image: "redis:7-alpine", internalPort: 6379 }),
  })
})
test.afterAll(() => srv.cleanup())

test("apps list renders rows and row click navigates to detail", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui`)

  // Table populates with both apps
  await expect(page.locator('tr[data-app-name="app-one"]')).toBeVisible()
  await expect(page.locator('tr[data-app-name="app-two"]')).toBeVisible()

  // Click a row → hash changes to app detail
  await page.click('tr[data-app-name="app-one"]')
  expect(new URL(page.url()).hash).toBe("#/apps/app-one")
})

test("refresh button reloads list", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui`)
  await expect(page.locator('tr[data-app-name="app-one"]')).toBeVisible()
  await page.click('button:has-text("Refresh")')
  // After refresh the rows are still present
  await expect(page.locator('tr[data-app-name="app-one"]')).toBeVisible()
  await expect(page.locator('tr[data-app-name="app-two"]')).toBeVisible()
})
```

- [ ] **Step 4: Run tests**

Run: `bun run test:e2e tests/playwright/apps-list.spec.ts && bun run typecheck`
Expected: 2 Playwright tests pass, typecheck passes.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agent/ui/index.html src/lib/agent/ui/app.js tests/playwright/apps-list.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): apps list view with refresh + row navigation

Task 3: #/apps renders a live table of apps from GET /apps, refresh
button re-fetches, clicking a row navigates to #/apps/:name (detail
stub until Task 4). Status badges and source labels derived client-side.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: App detail — Overview sub-tab

**Goal:** `#/apps/:name` fetches a single app via `GET /apps/:name` and renders an Overview pane (read-only): source, status, domains, env, volumes, oauth, restart policy, git/compose details. No actions yet. Logs sub-tab added in Task 6.

**Files:**
- Modify: `src/lib/agent/ui/index.html` (app detail template)
- Modify: `src/lib/agent/ui/app.js` (loadApp, selectedApp)
- Create: `tests/playwright/app-detail-overview.spec.ts`

---

- [ ] **Step 1: Replace the app detail stub with the full Overview template**

In `src/lib/agent/ui/index.html`, replace the block starting `<!-- App detail (stub — filled in Task 4) -->` with:

```html
          <!-- App detail -->
          <template x-if="route.view === 'apps' && route.param">
            <section>
              <div class="flex items-center gap-3 mb-4">
                <a href="#/apps" class="text-sm text-gray-500 hover:text-gray-700">← Apps</a>
                <h1 class="text-xl font-semibold" x-text="route.param"></h1>
                <template x-if="selectedApp">
                  <span :class="statusBadgeClass(selectedApp.status)" class="inline-block px-2 py-0.5 text-xs rounded font-medium" x-text="selectedApp.status"></span>
                </template>
              </div>

              <template x-if="selectedApp === null">
                <div class="text-sm text-gray-500">Loading…</div>
              </template>

              <template x-if="selectedApp === 'not-found'">
                <div class="text-sm text-gray-500">
                  App not found — it may have been removed.
                  <a href="#/apps" class="text-brand-blue hover:underline ml-1">Back to apps</a>.
                </div>
              </template>

              <template x-if="selectedApp && selectedApp !== 'not-found'">
                <div class="space-y-4">
                  <!-- Sub-tabs -->
                  <div class="border-b border-gray-200 flex gap-4">
                    <button @click="route.subtab = 'overview'"
                            :class="route.subtab === 'overview' || !route.subtab ? 'border-brand-blue text-gray-900 font-medium' : 'border-transparent text-gray-500'"
                            class="border-b-2 pb-2 text-sm">
                      Overview
                    </button>
                    <button @click="route.subtab = 'logs'"
                            :class="route.subtab === 'logs' ? 'border-brand-blue text-gray-900 font-medium' : 'border-transparent text-gray-500'"
                            class="border-b-2 pb-2 text-sm">
                      Logs
                    </button>
                  </div>

                  <!-- Overview pane -->
                  <template x-if="!route.subtab || route.subtab === 'overview'">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                      <div class="bg-white border border-gray-200 rounded p-4">
                        <div class="text-xs uppercase text-gray-500 mb-2">Source</div>
                        <div class="text-sm font-mono break-all">
                          <template x-if="selectedApp.compose">
                            <div>
                              compose (<span x-text="selectedApp.compose.source"></span>) · primary: <span x-text="selectedApp.compose.primaryService"></span>
                              <template x-if="selectedApp.compose.path"><div class="text-gray-600" x-text="'path: ' + selectedApp.compose.path"></div></template>
                            </div>
                          </template>
                          <template x-if="selectedApp.git && !selectedApp.compose">
                            <div>
                              <div x-text="selectedApp.git.repoUrl"></div>
                              <div class="text-gray-600" x-text="'branch: ' + selectedApp.git.branch + ' · dockerfile: ' + selectedApp.git.dockerfile"></div>
                              <template x-if="selectedApp.git.context"><div class="text-gray-600" x-text="'context: ' + selectedApp.git.context"></div></template>
                            </div>
                          </template>
                          <template x-if="selectedApp.dockerfile && !selectedApp.git">
                            <div>inline dockerfile</div>
                          </template>
                          <template x-if="!selectedApp.compose && !selectedApp.git && !selectedApp.dockerfile">
                            <div x-text="selectedApp.image"></div>
                          </template>
                        </div>
                      </div>

                      <div class="bg-white border border-gray-200 rounded p-4">
                        <div class="text-xs uppercase text-gray-500 mb-2">Routing</div>
                        <div class="text-sm">
                          <div><span class="text-gray-500">Internal port:</span> <span class="font-mono" x-text="selectedApp.internalPort"></span></div>
                          <div><span class="text-gray-500">Domains:</span> <span class="font-mono" x-text="(selectedApp.domains || []).join(', ') || '—'"></span></div>
                          <div><span class="text-gray-500">Restart policy:</span> <span class="font-mono" x-text="selectedApp.restartPolicy || 'unless-stopped'"></span></div>
                        </div>
                      </div>

                      <div class="bg-white border border-gray-200 rounded p-4 md:col-span-2">
                        <div class="text-xs uppercase text-gray-500 mb-2">Environment</div>
                        <template x-if="Object.keys(selectedApp.env || {}).length === 0">
                          <div class="text-sm text-gray-500">No env vars set.</div>
                        </template>
                        <template x-if="Object.keys(selectedApp.env || {}).length > 0">
                          <dl class="text-sm font-mono grid grid-cols-[max-content_1fr] gap-x-4 gap-y-1">
                            <template x-for="k in Object.keys(selectedApp.env).sort()" :key="k">
                              <template>
                                <dt class="text-gray-700" x-text="k"></dt>
                                <dd class="text-gray-900 break-all" x-text="selectedApp.env[k]"></dd>
                              </template>
                            </template>
                          </dl>
                        </template>
                      </div>

                      <div class="bg-white border border-gray-200 rounded p-4 md:col-span-2">
                        <div class="text-xs uppercase text-gray-500 mb-2">Volumes</div>
                        <template x-if="(selectedApp.volumes || []).length === 0">
                          <div class="text-sm text-gray-500">No volumes.</div>
                        </template>
                        <template x-if="(selectedApp.volumes || []).length > 0">
                          <ul class="text-sm font-mono space-y-1">
                            <template x-for="v in selectedApp.volumes" :key="v.name + ':' + v.mountPath">
                              <li x-text="v.name + ' → ' + v.mountPath + (v.readonly ? ' (ro)' : '')"></li>
                            </template>
                          </ul>
                        </template>
                      </div>

                      <div class="bg-white border border-gray-200 rounded p-4 md:col-span-2">
                        <div class="text-xs uppercase text-gray-500 mb-2">Access control</div>
                        <template x-if="!selectedApp.oauth">
                          <div class="text-sm text-gray-500">Public (no OAuth).</div>
                        </template>
                        <template x-if="selectedApp.oauth">
                          <div class="text-sm">
                            <template x-if="(selectedApp.oauth.allowedEmails || []).length"><div><span class="text-gray-500">Emails:</span> <span class="font-mono" x-text="selectedApp.oauth.allowedEmails.join(', ')"></span></div></template>
                            <template x-if="selectedApp.oauth.allowedDomain"><div><span class="text-gray-500">Domain:</span> <span class="font-mono" x-text="selectedApp.oauth.allowedDomain"></span></div></template>
                            <template x-if="(selectedApp.oauth.allowedGroups || []).length"><div><span class="text-gray-500">Groups:</span> <span class="font-mono" x-text="selectedApp.oauth.allowedGroups.join(', ')"></span></div></template>
                          </div>
                        </template>
                      </div>

                      <div class="bg-white border border-gray-200 rounded p-4 md:col-span-2">
                        <div class="text-xs uppercase text-gray-500 mb-2">Build / deploy state</div>
                        <div class="text-sm font-mono space-y-1">
                          <div><span class="text-gray-500">Created:</span> <span x-text="selectedApp.createdAt || '—'"></span></div>
                          <div><span class="text-gray-500">Deployed:</span> <span x-text="selectedApp.deployedAt || '—'"></span></div>
                          <template x-if="selectedApp.commitHash"><div><span class="text-gray-500">Commit:</span> <span x-text="selectedApp.commitHash"></span></div></template>
                          <template x-if="selectedApp.lastBuildAt"><div><span class="text-gray-500">Last build:</span> <span x-text="selectedApp.lastBuildAt"></span></div></template>
                          <template x-if="selectedApp.containerId"><div><span class="text-gray-500">Container:</span> <span x-text="selectedApp.containerId.substring(0, 12)"></span></div></template>
                        </div>
                      </div>
                    </div>
                  </template>

                  <!-- Logs sub-tab placeholder (Task 6) -->
                  <template x-if="route.subtab === 'logs'">
                    <div class="text-sm text-gray-500">Logs coming in Task 6.</div>
                  </template>
                </div>
              </template>
            </section>
          </template>
```

- [ ] **Step 2: Extend `app.js` with `loadApp` and wire `onRouteEnter`**

In `src/lib/agent/ui/app.js`, add after `loadApps`:

```js
    async loadApp(name) {
      this.selectedApp = null
      this.pending.add("app-detail")
      try {
        const res = await this.apiFetch("/apps/" + encodeURIComponent(name))
        if (res.status === 404) {
          this.selectedApp = "not-found"
          return
        }
        const body = await res.json()
        if (body.success) {
          this.selectedApp = body.data
        } else {
          this.selectedApp = "not-found"
          this.toast("error", body.error || "Failed to load app")
        }
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.toast("error", "Could not reach server")
        }
      } finally {
        this.pending.delete("app-detail")
      }
    },
```

Update `onRouteEnter` to branch on detail view:

```js
    onRouteEnter() {
      if (this.route.view === "apps" && !this.route.param) this.loadApps()
      if (this.route.view === "apps" && this.route.param) this.loadApp(this.route.param)
      // sites + groups wired in later tasks
    },
```

- [ ] **Step 3: Playwright test — app detail overview**

Create `tests/playwright/app-detail-overview.spec.ts`:

```ts
import { test, expect } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => {
  srv = await startTestServer({ apiKey: "right-key" })
  await fetch(`${srv.url}/apps`, {
    method: "POST",
    headers: { "X-API-Key": srv.apiKey, "Content-Type": "application/json" },
    body: JSON.stringify({
      name: "demo",
      image: "nginx:alpine",
      internalPort: 80,
      env: { FOO: "bar", NODE_ENV: "production" },
      restartPolicy: "always",
    }),
  })
})
test.afterAll(() => srv.cleanup())

test("app detail renders overview fields", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui#/apps/demo`)

  await expect(page.locator("h1", { hasText: "demo" })).toBeVisible()
  await expect(page.getByText("nginx:alpine")).toBeVisible()
  await expect(page.getByText("FOO")).toBeVisible()
  await expect(page.getByText("bar")).toBeVisible()
  await expect(page.getByText("NODE_ENV")).toBeVisible()
  await expect(page.getByText("always").first()).toBeVisible()
})

test("unknown app shows not-found empty state", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui#/apps/nope-does-not-exist`)
  await expect(page.getByText("App not found")).toBeVisible()
})
```

- [ ] **Step 4: Run tests**

Run: `bun run test:e2e tests/playwright/app-detail-overview.spec.ts && bun run typecheck`
Expected: 2 Playwright tests pass.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agent/ui/ tests/playwright/app-detail-overview.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): app detail overview (read-only)

Task 4: #/apps/:name fetches GET /apps/:name and renders a read-only
Overview pane (source, routing, env, volumes, oauth, build/deploy state).
404 renders a graceful 'not found' empty state.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: App lifecycle actions — deploy, stop, restart, remove

**Goal:** Action bar on the app detail view with four buttons: Deploy / Stop / Restart / Remove. Each calls the matching endpoint, pushes a pending flag, refetches on success, shows a toast. Destructive actions (Remove) ask for `confirm()`. Deploy uses fire-and-hold (no timeout, spinner).

**Files:**
- Modify: `src/lib/agent/ui/index.html` (action bar + toasts)
- Modify: `src/lib/agent/ui/app.js` (deployApp/stopApp/restartApp/removeApp)
- Create: `tests/playwright/app-actions.spec.ts`

---

- [ ] **Step 1: Add the action bar to the app detail header and a toast container**

In `src/lib/agent/ui/index.html`, update the app-detail header to include an action bar on the right. Replace the header `<div class="flex items-center gap-3 mb-4">...</div>` with:

```html
              <div class="flex items-center gap-3 mb-4 flex-wrap">
                <a href="#/apps" class="text-sm text-gray-500 hover:text-gray-700">← Apps</a>
                <h1 class="text-xl font-semibold" x-text="route.param"></h1>
                <template x-if="selectedApp && selectedApp !== 'not-found'">
                  <span :class="statusBadgeClass(selectedApp.status)" class="inline-block px-2 py-0.5 text-xs rounded font-medium" x-text="selectedApp.status"></span>
                </template>
                <div class="ml-auto flex items-center gap-2" x-show="selectedApp && selectedApp !== 'not-found'">
                  <button
                    :disabled="pending.has('deploy')"
                    @click="deployApp(route.param)"
                    class="inline-flex items-center gap-1 px-3 py-1.5 text-sm rounded border border-brand-blue text-brand-blue hover:bg-brand-blue/5 disabled:opacity-60">
                    <i class="hgi hgi-stroke hgi-rocket"></i>
                    <span x-show="!pending.has('deploy')">Deploy</span>
                    <span x-show="pending.has('deploy')">Deploying…</span>
                  </button>
                  <button
                    :disabled="pending.has('stop')"
                    @click="stopApp(route.param)"
                    class="inline-flex items-center gap-1 px-3 py-1.5 text-sm rounded border border-gray-200 hover:bg-gray-50 disabled:opacity-60">
                    <i class="hgi hgi-stroke hgi-stop-circle"></i> Stop
                  </button>
                  <button
                    :disabled="pending.has('restart')"
                    @click="restartApp(route.param)"
                    class="inline-flex items-center gap-1 px-3 py-1.5 text-sm rounded border border-gray-200 hover:bg-gray-50 disabled:opacity-60">
                    <i class="hgi hgi-stroke hgi-reload"></i> Restart
                  </button>
                  <button
                    :disabled="pending.has('remove')"
                    @click="removeApp(route.param)"
                    class="inline-flex items-center gap-1 px-3 py-1.5 text-sm rounded border border-red-200 text-red-700 hover:bg-red-50 disabled:opacity-60">
                    <i class="hgi hgi-stroke hgi-delete-02"></i> Remove
                  </button>
                </div>
              </div>
```

Add a toast container just before the closing `</body>` (inside `<body>` so it shares the root Alpine scope):

```html
  <div class="fixed bottom-4 right-4 z-50 space-y-2 pointer-events-none" aria-live="polite">
    <template x-for="t in toasts" :key="t.id">
      <div :class="t.type === 'error' ? 'bg-red-600' : (t.type === 'success' ? 'bg-brand-green' : 'bg-gray-900')"
           class="pointer-events-auto text-white text-sm px-3 py-2 rounded shadow-md min-w-[220px] max-w-sm">
        <span x-text="t.message"></span>
      </div>
    </template>
  </div>
```

- [ ] **Step 2: Implement the action methods in `app.js`**

In `src/lib/agent/ui/app.js`, add after `loadApp`:

```js
    async _runAction(name, key, method, path, successMsg) {
      this.pending.add(key)
      try {
        const res = await this.apiFetch(path, { method })
        const body = await res.json()
        if (!body.success) {
          this.toast("error", body.error || "Action failed")
          return
        }
        this.toast("success", successMsg)
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.toast("error", "Could not reach server")
        }
      } finally {
        this.pending.delete(key)
      }
    },

    async deployApp(name) {
      await this._runAction(name, "deploy", "POST", `/apps/${encodeURIComponent(name)}/deploy`, `App ${name} deployed`)
      await this.loadApp(name)
    },

    async stopApp(name) {
      await this._runAction(name, "stop", "POST", `/apps/${encodeURIComponent(name)}/stop`, `App ${name} stopped`)
      await this.loadApp(name)
    },

    async restartApp(name) {
      await this._runAction(name, "restart", "POST", `/apps/${encodeURIComponent(name)}/restart`, `App ${name} restarted`)
      await this.loadApp(name)
    },

    async removeApp(name) {
      if (!confirm(`Remove app '${name}'? Container and image will be deleted.`)) return
      await this._runAction(name, "remove", "DELETE", `/apps/${encodeURIComponent(name)}`, `App ${name} removed`)
      // After removal, navigate back to the list
      window.location.hash = "#/apps"
    },
```

- [ ] **Step 3: Playwright test — verify each button hits the expected endpoint**

Create `tests/playwright/app-actions.spec.ts`:

```ts
import { test, expect, type Route } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => {
  srv = await startTestServer({ apiKey: "right-key" })
  await fetch(`${srv.url}/apps`, {
    method: "POST",
    headers: { "X-API-Key": srv.apiKey, "Content-Type": "application/json" },
    body: JSON.stringify({ name: "actionable", image: "nginx:alpine", internalPort: 80 }),
  })
})
test.afterAll(() => srv.cleanup())

async function interceptActionEndpoints(page: any, capture: { path: string; method: string }[]) {
  // Intercept deploy/stop/restart/delete on this app and return a fake success
  const routes: string[] = [
    `${srv.url}/apps/actionable/deploy`,
    `${srv.url}/apps/actionable/stop`,
    `${srv.url}/apps/actionable/restart`,
    `${srv.url}/apps/actionable`,
  ]
  for (const url of routes) {
    await page.route(url, async (route: Route) => {
      const req = route.request()
      capture.push({ path: new URL(req.url()).pathname, method: req.method() })
      await route.fulfill({
        status: 200,
        contentType: "application/json",
        body: JSON.stringify({ success: true, data: { name: "actionable", status: "running" } }),
      })
    })
  }
}

test("deploy button calls POST /apps/:name/deploy", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  const capture: { path: string; method: string }[] = []
  await interceptActionEndpoints(page, capture)
  await page.goto(`${srv.url}/ui#/apps/actionable`)
  await page.click('button:has-text("Deploy")')
  await expect.poll(() => capture.find(c => c.path === "/apps/actionable/deploy" && c.method === "POST")).toBeTruthy()
})

test("stop button calls POST /apps/:name/stop", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  const capture: { path: string; method: string }[] = []
  await interceptActionEndpoints(page, capture)
  await page.goto(`${srv.url}/ui#/apps/actionable`)
  await page.click('button:has-text("Stop")')
  await expect.poll(() => capture.find(c => c.path === "/apps/actionable/stop" && c.method === "POST")).toBeTruthy()
})

test("restart button calls POST /apps/:name/restart", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  const capture: { path: string; method: string }[] = []
  await interceptActionEndpoints(page, capture)
  await page.goto(`${srv.url}/ui#/apps/actionable`)
  await page.click('button:has-text("Restart")')
  await expect.poll(() => capture.find(c => c.path === "/apps/actionable/restart" && c.method === "POST")).toBeTruthy()
})

test("remove button confirms then calls DELETE /apps/:name and returns to list", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  const capture: { path: string; method: string }[] = []
  await interceptActionEndpoints(page, capture)
  page.on("dialog", (d) => d.accept())
  await page.goto(`${srv.url}/ui#/apps/actionable`)
  await page.click('button:has-text("Remove")')
  await expect.poll(() => capture.find(c => c.path === "/apps/actionable" && c.method === "DELETE")).toBeTruthy()
  // Redirected to list
  await expect.poll(() => new URL(page.url()).hash).toBe("#/apps")
})
```

- [ ] **Step 4: Run tests**

Run: `bun run test:e2e tests/playwright/app-actions.spec.ts && bun run typecheck`
Expected: 4 Playwright tests pass.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agent/ui/ tests/playwright/app-actions.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): app lifecycle actions + toasts

Task 5: deploy / stop / restart / remove buttons on the app detail
page. Fire-and-hold deploy (no timeout), pending spinners, success
or error toast, detail refetch on success. Remove uses confirm() and
redirects to the apps list.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: App logs — polling sub-tab

**Goal:** Logs sub-tab on the app detail view. Fetches `GET /apps/:name/logs?tail=200` and renders the text in a monospace `<pre>`. Auto-refresh every 3 seconds by default, toggleable; paused when the browser tab is hidden. Scroll locks to bottom while auto-refresh is on.

**Files:**
- Modify: `src/lib/agent/ui/index.html` (logs pane markup)
- Modify: `src/lib/agent/ui/app.js` (loadAppLogs, polling lifecycle)
- Create: `tests/playwright/app-logs.spec.ts`

---

- [ ] **Step 1: Replace the logs placeholder with the real logs pane**

In `src/lib/agent/ui/index.html`, replace the `<!-- Logs sub-tab placeholder (Task 6) -->` block with:

```html
                  <template x-if="route.subtab === 'logs'">
                    <div>
                      <div class="flex items-center gap-3 mb-2">
                        <label class="inline-flex items-center gap-2 text-sm text-gray-700">
                          <input type="checkbox" x-model="appLogsAuto" @change="appLogsAuto ? startLogsPoll() : stopLogsPoll()">
                          Auto-refresh (3s)
                        </label>
                        <button
                          @click="loadAppLogs(route.param)"
                          :disabled="pending.has('logs')"
                          class="ml-auto inline-flex items-center gap-1 px-3 py-1 text-sm border border-gray-200 rounded hover:bg-gray-50 disabled:opacity-50">
                          <i class="hgi hgi-stroke hgi-refresh"></i> Refresh now
                        </button>
                      </div>
                      <pre class="logs" x-ref="logsEl" x-text="appLogs || 'No logs yet.'"></pre>
                    </div>
                  </template>
```

- [ ] **Step 2: Add logs state, polling, visibility handling**

In `src/lib/agent/ui/app.js`, add these fields to the returned object (near other data fields):

```js
    // logs
    appLogs: "",
    appLogsAuto: true,
    appLogsTimer: null,
    _logsVisibilityHandler: null,
```

Then add these methods after `removeApp`:

```js
    async loadAppLogs(name) {
      this.pending.add("logs")
      try {
        const res = await this.apiFetch(`/apps/${encodeURIComponent(name)}/logs?tail=200`)
        const body = await res.json()
        if (body.success) {
          this.appLogs = body.data.logs || ""
          // Scroll to bottom if auto-refresh is on
          this.$nextTick(() => {
            if (this.appLogsAuto && this.$refs.logsEl) {
              this.$refs.logsEl.scrollTop = this.$refs.logsEl.scrollHeight
            }
          })
        } else {
          this.toast("error", body.error || "Failed to load logs")
        }
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.toast("error", "Could not reach server")
        }
      } finally {
        this.pending.delete("logs")
      }
    },

    startLogsPoll() {
      this.stopLogsPoll()
      const name = this.route.param
      if (!name) return
      this.loadAppLogs(name)
      this.appLogsTimer = setInterval(() => {
        if (document.hidden) return
        this.loadAppLogs(name)
      }, 3000)
      this._logsVisibilityHandler = () => {
        // When page comes back to foreground, fetch immediately
        if (!document.hidden && this.route.subtab === "logs" && this.appLogsAuto) {
          this.loadAppLogs(this.route.param)
        }
      }
      document.addEventListener("visibilitychange", this._logsVisibilityHandler)
    },

    stopLogsPoll() {
      if (this.appLogsTimer) {
        clearInterval(this.appLogsTimer)
        this.appLogsTimer = null
      }
      if (this._logsVisibilityHandler) {
        document.removeEventListener("visibilitychange", this._logsVisibilityHandler)
        this._logsVisibilityHandler = null
      }
    },
```

Now wire the polling lifecycle to sub-tab changes. Modify `parseHash` so it parses an optional sub-tab:

```js
    parseHash() {
      const h = window.location.hash.replace(/^#/, "") || "/apps"
      const parts = h.split("/").filter(Boolean)
      const view = parts[0] || "apps"
      const param = parts[1] || null
      const subtab = parts[2] || null
      // When leaving a logs tab (or any view change), stop any poll
      if (this.route.subtab === "logs" && subtab !== "logs") this.stopLogsPoll()
      this.route = { view, param, subtab }
      if (this.authed) this.onRouteEnter()
    },
```

Update `onRouteEnter` so entering the logs sub-tab kicks off polling (if enabled), and make the sub-tab buttons drive the URL via hash so the state survives reloads. Replace the two sub-tab buttons in `index.html` with:

```html
                    <button @click="window.location.hash = '#/apps/' + route.param + '/overview'"
                            :class="(route.subtab === 'overview' || !route.subtab) ? 'border-brand-blue text-gray-900 font-medium' : 'border-transparent text-gray-500'"
                            class="border-b-2 pb-2 text-sm">
                      Overview
                    </button>
                    <button @click="window.location.hash = '#/apps/' + route.param + '/logs'"
                            :class="route.subtab === 'logs' ? 'border-brand-blue text-gray-900 font-medium' : 'border-transparent text-gray-500'"
                            class="border-b-2 pb-2 text-sm">
                      Logs
                    </button>
```

Update `onRouteEnter` in `app.js`:

```js
    onRouteEnter() {
      if (this.route.view === "apps" && !this.route.param) this.loadApps()
      if (this.route.view === "apps" && this.route.param) {
        // Only re-fetch the app detail when we arrive on a new app (not on sub-tab change)
        if (!this.selectedApp || (this.selectedApp && this.selectedApp.name !== this.route.param)) {
          this.loadApp(this.route.param)
        }
        if (this.route.subtab === "logs") {
          if (this.appLogsAuto) this.startLogsPoll()
          else this.loadAppLogs(this.route.param)
        }
      }
    },
```

- [ ] **Step 3: Playwright test — logs pane**

Create `tests/playwright/app-logs.spec.ts`:

```ts
import { test, expect, type Route } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => {
  srv = await startTestServer({ apiKey: "right-key" })
  await fetch(`${srv.url}/apps`, {
    method: "POST",
    headers: { "X-API-Key": srv.apiKey, "Content-Type": "application/json" },
    body: JSON.stringify({ name: "logsapp", image: "nginx:alpine", internalPort: 80 }),
  })
})
test.afterAll(() => srv.cleanup())

test("logs pane fetches and renders tail", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  // Intercept /apps/logsapp/logs to return canned output
  let hitCount = 0
  await page.route(`${srv.url}/apps/logsapp/logs?*`, async (route: Route) => {
    hitCount++
    await route.fulfill({
      status: 200,
      contentType: "application/json",
      body: JSON.stringify({ success: true, data: { name: "logsapp", logs: `line from server #${hitCount}\n`, lines: 200 } }),
    })
  })

  await page.goto(`${srv.url}/ui#/apps/logsapp/logs`)
  await expect(page.locator("pre.logs")).toContainText("line from server")
  // With auto-refresh on (default), at least 2 requests after ~4 seconds
  await page.waitForTimeout(4000)
  expect(hitCount).toBeGreaterThan(1)
})

test("pausing auto-refresh stops polling", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  let hitCount = 0
  await page.route(`${srv.url}/apps/logsapp/logs?*`, async (route: Route) => {
    hitCount++
    await route.fulfill({
      status: 200,
      contentType: "application/json",
      body: JSON.stringify({ success: true, data: { name: "logsapp", logs: "hello\n", lines: 200 } }),
    })
  })

  await page.goto(`${srv.url}/ui#/apps/logsapp/logs`)
  // Uncheck the auto-refresh checkbox
  await page.uncheck('input[type="checkbox"]')
  const before = hitCount
  await page.waitForTimeout(4000)
  // No additional polls after pause
  expect(hitCount).toBeLessThanOrEqual(before + 1) // allow 1 in-flight
})
```

- [ ] **Step 4: Run tests**

Run: `bun run test:e2e tests/playwright/app-logs.spec.ts && bun run typecheck`
Expected: 2 Playwright tests pass.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agent/ui/ tests/playwright/app-logs.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): app logs sub-tab with 3s polling

Task 6: Logs sub-tab fetches GET /apps/:name/logs?tail=200 on enter and
polls every 3s when auto-refresh is on. Polling pauses on tab-hidden
(Page Visibility API) and when the user unchecks the box. Scroll locks
to the bottom on each refresh.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Sites list + detail (undeploy, rollback)

**Goal:** `#/sites` shows a table of sites. `#/sites/:subdomain` shows Overview + History sub-tabs. Undeploy button on detail (with `confirm()`), per-version Rollback button in History.

**Files:**
- Modify: `src/lib/agent/ui/index.html`
- Modify: `src/lib/agent/ui/app.js`
- Create: `tests/playwright/sites.spec.ts`

---

- [ ] **Step 1: Replace the sites placeholder with the list + detail markup**

In `src/lib/agent/ui/index.html`, replace the `<!-- Sites placeholder -->` block with:

```html
          <!-- Sites list -->
          <template x-if="route.view === 'sites' && !route.param">
            <section>
              <div class="flex items-center justify-between mb-4">
                <h1 class="text-xl font-semibold">Sites</h1>
                <button @click="loadSites()" :disabled="pending.has('sites-list')"
                        class="inline-flex items-center gap-1 px-3 py-1.5 text-sm border border-gray-200 rounded hover:bg-gray-50 disabled:opacity-50">
                  <i class="hgi hgi-stroke hgi-refresh"></i>
                  <span x-show="!pending.has('sites-list')">Refresh</span>
                  <span x-show="pending.has('sites-list')">Loading…</span>
                </button>
              </div>

              <template x-if="sites === null"><div class="text-sm text-gray-500">Loading sites…</div></template>
              <template x-if="sites && sites.length === 0"><div class="text-sm text-gray-500">No sites deployed.</div></template>

              <template x-if="sites && sites.length > 0">
                <div class="border border-gray-200 rounded overflow-hidden bg-white">
                  <table class="w-full text-sm">
                    <thead class="bg-gray-50 text-left text-xs uppercase text-gray-600">
                      <tr>
                        <th class="px-3 py-2">Subdomain</th>
                        <th class="px-3 py-2">Version</th>
                        <th class="px-3 py-2">Size</th>
                        <th class="px-3 py-2">Deployed</th>
                        <th class="px-3 py-2">TLS</th>
                        <th class="px-3 py-2"></th>
                      </tr>
                    </thead>
                    <tbody>
                      <template x-for="s in sites" :key="s.subdomain">
                        <tr class="border-t border-gray-100 hover:bg-gray-50 cursor-pointer"
                            @click="window.location.hash = '#/sites/' + s.subdomain"
                            :data-site-subdomain="s.subdomain">
                          <td class="px-3 py-2 font-medium" x-text="s.subdomain"></td>
                          <td class="px-3 py-2 font-mono text-gray-700" x-text="s.version || '—'"></td>
                          <td class="px-3 py-2 text-gray-700" x-text="formatBytes(s.size)"></td>
                          <td class="px-3 py-2 text-gray-700" x-text="s.deployedAt"></td>
                          <td class="px-3 py-2 text-gray-700 text-xs uppercase" x-text="s.tls || '—'"></td>
                          <td class="px-3 py-2 text-right">
                            <a :href="s.url" target="_blank" rel="noreferrer"
                               @click.stop
                               class="text-brand-blue hover:underline text-xs">Open ↗</a>
                          </td>
                        </tr>
                      </template>
                    </tbody>
                  </table>
                </div>
              </template>
            </section>
          </template>

          <!-- Site detail -->
          <template x-if="route.view === 'sites' && route.param">
            <section>
              <div class="flex items-center gap-3 mb-4 flex-wrap">
                <a href="#/sites" class="text-sm text-gray-500 hover:text-gray-700">← Sites</a>
                <h1 class="text-xl font-semibold" x-text="route.param"></h1>
                <div class="ml-auto" x-show="selectedSite && selectedSite !== 'not-found'">
                  <button
                    :disabled="pending.has('undeploy')"
                    @click="undeploySite(route.param)"
                    class="inline-flex items-center gap-1 px-3 py-1.5 text-sm rounded border border-red-200 text-red-700 hover:bg-red-50 disabled:opacity-60">
                    <i class="hgi hgi-stroke hgi-delete-02"></i> Undeploy
                  </button>
                </div>
              </div>

              <template x-if="selectedSite === null"><div class="text-sm text-gray-500">Loading…</div></template>
              <template x-if="selectedSite === 'not-found'">
                <div class="text-sm text-gray-500">
                  Site not found.
                  <a href="#/sites" class="text-brand-blue hover:underline ml-1">Back to sites</a>.
                </div>
              </template>

              <template x-if="selectedSite && selectedSite !== 'not-found'">
                <div class="space-y-4">
                  <div class="border-b border-gray-200 flex gap-4">
                    <button @click="window.location.hash = '#/sites/' + route.param + '/overview'"
                            :class="(route.subtab === 'overview' || !route.subtab) ? 'border-brand-blue text-gray-900 font-medium' : 'border-transparent text-gray-500'"
                            class="border-b-2 pb-2 text-sm">Overview</button>
                    <button @click="window.location.hash = '#/sites/' + route.param + '/history'"
                            :class="route.subtab === 'history' ? 'border-brand-blue text-gray-900 font-medium' : 'border-transparent text-gray-500'"
                            class="border-b-2 pb-2 text-sm">History</button>
                  </div>

                  <template x-if="!route.subtab || route.subtab === 'overview'">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                      <div class="bg-white border border-gray-200 rounded p-4">
                        <div class="text-xs uppercase text-gray-500 mb-2">URL</div>
                        <a :href="selectedSite.url" target="_blank" rel="noreferrer" class="text-brand-blue hover:underline font-mono text-sm break-all" x-text="selectedSite.url"></a>
                      </div>
                      <div class="bg-white border border-gray-200 rounded p-4">
                        <div class="text-xs uppercase text-gray-500 mb-2">Deployed</div>
                        <div class="text-sm font-mono" x-text="selectedSite.deployedAt"></div>
                        <div class="text-sm"><span class="text-gray-500">Version:</span> <span class="font-mono" x-text="selectedSite.version || '—'"></span></div>
                        <div class="text-sm"><span class="text-gray-500">Size:</span> <span class="font-mono" x-text="formatBytes(selectedSite.size)"></span></div>
                        <div class="text-sm"><span class="text-gray-500">TLS:</span> <span class="font-mono" x-text="selectedSite.tls || '—'"></span></div>
                      </div>
                      <div class="bg-white border border-gray-200 rounded p-4 md:col-span-2">
                        <div class="text-xs uppercase text-gray-500 mb-2">Custom domains</div>
                        <div class="text-sm font-mono" x-text="(selectedSite.domains || []).join(', ') || '—'"></div>
                      </div>
                      <div class="bg-white border border-gray-200 rounded p-4 md:col-span-2">
                        <div class="text-xs uppercase text-gray-500 mb-2">Access control</div>
                        <template x-if="!selectedSite.oauth"><div class="text-sm text-gray-500">Public</div></template>
                        <template x-if="selectedSite.oauth">
                          <div class="text-sm">
                            <template x-if="(selectedSite.oauth.allowedEmails || []).length"><div><span class="text-gray-500">Emails:</span> <span class="font-mono" x-text="selectedSite.oauth.allowedEmails.join(', ')"></span></div></template>
                            <template x-if="selectedSite.oauth.allowedDomain"><div><span class="text-gray-500">Domain:</span> <span class="font-mono" x-text="selectedSite.oauth.allowedDomain"></span></div></template>
                            <template x-if="(selectedSite.oauth.allowedGroups || []).length"><div><span class="text-gray-500">Groups:</span> <span class="font-mono" x-text="selectedSite.oauth.allowedGroups.join(', ')"></span></div></template>
                          </div>
                        </template>
                      </div>
                    </div>
                  </template>

                  <template x-if="route.subtab === 'history'">
                    <div>
                      <template x-if="siteHistory === null"><div class="text-sm text-gray-500">Loading history…</div></template>
                      <template x-if="siteHistory && siteHistory.length === 0"><div class="text-sm text-gray-500">No history.</div></template>
                      <template x-if="siteHistory && siteHistory.length > 0">
                        <div class="border border-gray-200 rounded overflow-hidden bg-white">
                          <table class="w-full text-sm">
                            <thead class="bg-gray-50 text-left text-xs uppercase text-gray-600">
                              <tr>
                                <th class="px-3 py-2">Version</th>
                                <th class="px-3 py-2">Deployed</th>
                                <th class="px-3 py-2">By</th>
                                <th class="px-3 py-2">Size</th>
                                <th class="px-3 py-2"></th>
                              </tr>
                            </thead>
                            <tbody>
                              <template x-for="v in siteHistory" :key="v.version">
                                <tr class="border-t border-gray-100" :data-version="v.version">
                                  <td class="px-3 py-2 font-mono" x-text="v.version"></td>
                                  <td class="px-3 py-2 font-mono text-gray-700" x-text="v.deployedAt"></td>
                                  <td class="px-3 py-2 text-gray-700" x-text="v.deployedBy || '—'"></td>
                                  <td class="px-3 py-2 text-gray-700" x-text="formatBytes(v.size)"></td>
                                  <td class="px-3 py-2 text-right">
                                    <button
                                      :disabled="pending.has('rollback-' + v.version) || v.version === selectedSite.version"
                                      @click="rollbackSite(route.param, v.version)"
                                      class="px-2 py-1 text-xs border border-gray-200 rounded hover:bg-gray-50 disabled:opacity-40 disabled:cursor-not-allowed">
                                      <template x-if="v.version === selectedSite.version"><span>current</span></template>
                                      <template x-if="v.version !== selectedSite.version"><span>Rollback</span></template>
                                    </button>
                                  </td>
                                </tr>
                              </template>
                            </tbody>
                          </table>
                        </div>
                      </template>
                    </div>
                  </template>
                </div>
              </template>
            </section>
          </template>
```

- [ ] **Step 2: Add sites state + methods + `formatBytes`**

In `src/lib/agent/ui/app.js`, add new fields near the other data fields:

```js
    siteHistory: null,
```

Add methods after the app ones:

```js
    // --- Sites ---

    async loadSites() {
      this.pending.add("sites-list")
      try {
        const res = await this.apiFetch("/sites")
        const body = await res.json()
        this.sites = body.success ? body.data : []
        if (!body.success) this.toast("error", body.error || "Failed to load sites")
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.sites = []
          this.toast("error", "Could not reach server")
        }
      } finally {
        this.pending.delete("sites-list")
      }
    },

    async loadSite(subdomain) {
      // Site detail fetched by listing and picking (no GET /sites/:subdomain exists).
      this.selectedSite = null
      try {
        const res = await this.apiFetch("/sites")
        const body = await res.json()
        if (!body.success) { this.selectedSite = "not-found"; return }
        const match = (body.data || []).find(s => s.subdomain === subdomain)
        this.selectedSite = match || "not-found"
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.toast("error", "Could not reach server")
        }
      }
    },

    async loadSiteHistory(subdomain) {
      this.siteHistory = null
      try {
        const res = await this.apiFetch(`/sites/${encodeURIComponent(subdomain)}/history`)
        if (res.status === 404) { this.siteHistory = []; return }
        const body = await res.json()
        this.siteHistory = body.success ? body.data : []
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.siteHistory = []
          this.toast("error", "Could not reach server")
        }
      }
    },

    async undeploySite(subdomain) {
      if (!confirm(`Undeploy site '${subdomain}'? Files will be deleted.`)) return
      this.pending.add("undeploy")
      try {
        const res = await this.apiFetch(`/sites/${encodeURIComponent(subdomain)}`, { method: "DELETE" })
        const body = await res.json()
        if (!body.success) {
          this.toast("error", body.error || "Failed to undeploy")
          return
        }
        this.toast("success", `Site ${subdomain} undeployed`)
        window.location.hash = "#/sites"
      } catch (err) {
        if (err && err.message !== "Unauthenticated") this.toast("error", "Could not reach server")
      } finally {
        this.pending.delete("undeploy")
      }
    },

    async rollbackSite(subdomain, version) {
      this.pending.add("rollback-" + version)
      try {
        const res = await this.apiFetch(`/sites/${encodeURIComponent(subdomain)}/rollback`, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ version }),
        })
        const body = await res.json()
        if (!body.success) {
          this.toast("error", body.error || "Rollback failed")
          return
        }
        this.toast("success", `Rolled back to v${version}`)
        await this.loadSite(subdomain)
        await this.loadSiteHistory(subdomain)
      } catch (err) {
        if (err && err.message !== "Unauthenticated") this.toast("error", "Could not reach server")
      } finally {
        this.pending.delete("rollback-" + version)
      }
    },

    formatBytes(n) {
      if (n === undefined || n === null) return "—"
      if (n < 1024) return n + " B"
      if (n < 1024 * 1024) return (n / 1024).toFixed(1) + " KB"
      if (n < 1024 * 1024 * 1024) return (n / 1024 / 1024).toFixed(1) + " MB"
      return (n / 1024 / 1024 / 1024).toFixed(1) + " GB"
    },
```

Extend `onRouteEnter` to cover sites:

```js
    onRouteEnter() {
      if (this.route.view === "apps" && !this.route.param) this.loadApps()
      if (this.route.view === "apps" && this.route.param) {
        if (!this.selectedApp || (this.selectedApp && this.selectedApp.name !== this.route.param)) {
          this.loadApp(this.route.param)
        }
        if (this.route.subtab === "logs") {
          if (this.appLogsAuto) this.startLogsPoll()
          else this.loadAppLogs(this.route.param)
        }
      }
      if (this.route.view === "sites" && !this.route.param) this.loadSites()
      if (this.route.view === "sites" && this.route.param) {
        if (!this.selectedSite || (this.selectedSite && this.selectedSite !== "not-found" && this.selectedSite.subdomain !== this.route.param)) {
          this.loadSite(this.route.param)
        }
        if (this.route.subtab === "history") this.loadSiteHistory(this.route.param)
      }
    },
```

- [ ] **Step 3: Playwright test — sites list, detail, rollback**

Create `tests/playwright/sites.spec.ts`:

```ts
import { test, expect } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

async function deploySite(url: string, apiKey: string, subdomain: string, files: Record<string, string>) {
  // Build a zip in memory using the fflate dependency already in the project
  const { zipSync, strToU8 } = await import("fflate")
  const entries: Record<string, Uint8Array> = {}
  for (const [k, v] of Object.entries(files)) entries[k] = strToU8(v)
  const zip = zipSync(entries)
  const res = await fetch(`${url}/sites/${subdomain}`, {
    method: "POST",
    headers: { "X-API-Key": apiKey, "Content-Type": "application/zip" },
    body: zip,
  })
  if (!res.ok) throw new Error(`deploySite failed: ${res.status} ${await res.text()}`)
}

test.beforeAll(async () => {
  srv = await startTestServer({ apiKey: "right-key" })
  await deploySite(srv.url, srv.apiKey, "alpha", { "index.html": "<h1>v1</h1>" })
  await deploySite(srv.url, srv.apiKey, "alpha", { "index.html": "<h1>v2</h1>" })
  await deploySite(srv.url, srv.apiKey, "beta",  { "index.html": "<h1>beta</h1>" })
})
test.afterAll(() => srv.cleanup())

test("sites list renders rows", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui#/sites`)
  await expect(page.locator('tr[data-site-subdomain="alpha"]')).toBeVisible()
  await expect(page.locator('tr[data-site-subdomain="beta"]')).toBeVisible()
})

test("sites list row click navigates to detail", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui#/sites`)
  await page.click('tr[data-site-subdomain="alpha"]')
  expect(new URL(page.url()).hash).toBe("#/sites/alpha")
  await expect(page.locator("h1", { hasText: "alpha" })).toBeVisible()
})

test("history sub-tab shows versions; rollback button visible on non-current", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui#/sites/alpha/history`)
  await expect(page.locator('tr[data-version="1"]')).toBeVisible()
  await expect(page.locator('tr[data-version="2"]')).toBeVisible()
  // Version 2 is current (last deployed)
  await expect(page.locator('tr[data-version="2"] button', { hasText: "current" })).toBeVisible()
  // Version 1 has a Rollback button
  const rollbackBtn = page.locator('tr[data-version="1"] button', { hasText: "Rollback" })
  await expect(rollbackBtn).toBeEnabled()
  await rollbackBtn.click()
  // After rollback, version 1 becomes current and version 2 gets the Rollback label
  await expect(page.locator('tr[data-version="1"] button', { hasText: "current" })).toBeVisible()
  await expect(page.locator('tr[data-version="2"] button', { hasText: "Rollback" })).toBeVisible()
})

test("undeploy removes site and returns to list", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  page.on("dialog", (d) => d.accept())
  await page.goto(`${srv.url}/ui#/sites/beta`)
  await page.click('button:has-text("Undeploy")')
  await expect.poll(() => new URL(page.url()).hash).toBe("#/sites")
  await expect(page.locator('tr[data-site-subdomain="beta"]')).toHaveCount(0)
})
```

- [ ] **Step 4: Run tests**

Run: `bun run test:e2e tests/playwright/sites.spec.ts && bun run typecheck`
Expected: 4 Playwright tests pass.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agent/ui/ tests/playwright/sites.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): sites list + detail with history + rollback + undeploy

Task 7: #/sites list with per-site row, #/sites/:subdomain Overview and
History sub-tabs, rollback per historical version, undeploy with
confirm().

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Groups list (read-only)

**Goal:** `#/groups` shows a read-only table of groups with an expandable members list per row. No mutations.

**Files:**
- Modify: `src/lib/agent/ui/index.html`
- Modify: `src/lib/agent/ui/app.js`
- Create: `tests/playwright/groups.spec.ts`

---

- [ ] **Step 1: Replace the groups placeholder with the list markup**

In `src/lib/agent/ui/index.html`, replace the `<!-- Groups placeholder -->` block with:

```html
          <!-- Groups -->
          <template x-if="route.view === 'groups'">
            <section x-data="{ expanded: {} }">
              <div class="flex items-center justify-between mb-4">
                <h1 class="text-xl font-semibold">Groups</h1>
                <button @click="loadGroups()" :disabled="pending.has('groups-list')"
                        class="inline-flex items-center gap-1 px-3 py-1.5 text-sm border border-gray-200 rounded hover:bg-gray-50 disabled:opacity-50">
                  <i class="hgi hgi-stroke hgi-refresh"></i>
                  <span x-show="!pending.has('groups-list')">Refresh</span>
                  <span x-show="pending.has('groups-list')">Loading…</span>
                </button>
              </div>

              <template x-if="groups === null"><div class="text-sm text-gray-500">Loading groups…</div></template>
              <template x-if="groups && groups.length === 0"><div class="text-sm text-gray-500">No groups defined.</div></template>

              <template x-if="groups && groups.length > 0">
                <div class="border border-gray-200 rounded overflow-hidden bg-white">
                  <table class="w-full text-sm">
                    <thead class="bg-gray-50 text-left text-xs uppercase text-gray-600">
                      <tr>
                        <th class="px-3 py-2">Name</th>
                        <th class="px-3 py-2">Members</th>
                      </tr>
                    </thead>
                    <tbody>
                      <template x-for="g in groups" :key="g.name">
                        <template>
                          <tr class="border-t border-gray-100 hover:bg-gray-50 cursor-pointer"
                              @click="expanded[g.name] = !expanded[g.name]"
                              :data-group-name="g.name">
                            <td class="px-3 py-2 font-medium" x-text="g.name"></td>
                            <td class="px-3 py-2 text-gray-700" x-text="(g.emails || []).length"></td>
                          </tr>
                          <template x-if="expanded[g.name]">
                            <tr class="border-t border-gray-100 bg-gray-50">
                              <td colspan="2" class="px-3 py-2">
                                <template x-if="(g.emails || []).length === 0">
                                  <div class="text-sm text-gray-500">No members.</div>
                                </template>
                                <template x-if="(g.emails || []).length > 0">
                                  <ul class="text-sm font-mono space-y-1">
                                    <template x-for="e in g.emails" :key="e"><li x-text="e"></li></template>
                                  </ul>
                                </template>
                              </td>
                            </tr>
                          </template>
                        </template>
                      </template>
                    </tbody>
                  </table>
                </div>
              </template>
            </section>
          </template>
```

- [ ] **Step 2: Add `loadGroups` and wire `onRouteEnter`**

In `src/lib/agent/ui/app.js`, add after `formatBytes`:

```js
    // --- Groups ---

    async loadGroups() {
      this.pending.add("groups-list")
      try {
        const res = await this.apiFetch("/groups")
        const body = await res.json()
        this.groups = body.success ? body.data : []
        if (!body.success) this.toast("error", body.error || "Failed to load groups")
      } catch (err) {
        if (err && err.message !== "Unauthenticated") {
          this.groups = []
          this.toast("error", "Could not reach server")
        }
      } finally {
        this.pending.delete("groups-list")
      }
    },
```

Extend `onRouteEnter`:

```js
      if (this.route.view === "groups") this.loadGroups()
```

(Add that line at the end of the function body.)

- [ ] **Step 3: Playwright test — groups list**

Create `tests/playwright/groups.spec.ts`:

```ts
import { test, expect } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => {
  srv = await startTestServer({ apiKey: "right-key" })
  await fetch(`${srv.url}/groups`, {
    method: "POST",
    headers: { "X-API-Key": srv.apiKey, "Content-Type": "application/json" },
    body: JSON.stringify({ name: "engineers", emails: ["a@example.com", "b@example.com"] }),
  })
  await fetch(`${srv.url}/groups`, {
    method: "POST",
    headers: { "X-API-Key": srv.apiKey, "Content-Type": "application/json" },
    body: JSON.stringify({ name: "ops", emails: [] }),
  })
})
test.afterAll(() => srv.cleanup())

test("groups list renders rows and expands on click", async ({ page, context }) => {
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui#/groups`)
  await expect(page.locator('tr[data-group-name="engineers"]')).toBeVisible()
  await expect(page.locator('tr[data-group-name="ops"]')).toBeVisible()
  // Click expands the row to reveal emails
  await page.click('tr[data-group-name="engineers"]')
  await expect(page.getByText("a@example.com")).toBeVisible()
  await expect(page.getByText("b@example.com")).toBeVisible()
})
```

- [ ] **Step 4: Run tests**

Run: `bun run test:e2e tests/playwright/groups.spec.ts && bun run typecheck`
Expected: 1 Playwright test passes.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agent/ui/ tests/playwright/groups.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): groups list (read-only, expandable rows)

Task 8: #/groups lists groups via GET /groups; clicking a row expands
inline to show the member emails. Read-only — no create/edit/delete.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Polish pass

**Goal:** Tighten rough edges: empty states on all list views, graceful "Retry" action on network-error states, `Esc` closes any inline confirm (we use native `confirm` so this is effectively `page.on("dialog")` only, but we add an Esc handler that cancels the Logs polling if the user is on that pane and hits Esc), favicon, focus management on login.

**Files:**
- Modify: `src/lib/agent/ui/index.html` (favicon, focus attr, small polish bits)
- Modify: `src/lib/agent/ui/app.js` (network-retry state, keyboard)
- Create: `tests/playwright/polish.spec.ts`

---

- [ ] **Step 1: Add a favicon link + focus autofocus on login**

At the top of `src/lib/agent/ui/index.html` `<head>`, add:

```html
  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'><rect width='32' height='32' rx='6' fill='%230969da'/><text x='50%25' y='55%25' dominant-baseline='middle' text-anchor='middle' fill='white' font-family='system-ui' font-size='18' font-weight='700'>s</text></svg>" />
```

Change the login input to autofocus when the login view mounts:

```html
          <input
            id="apiKeyInput"
            type="password"
            x-model="apiKeyInput"
            x-init="$nextTick(() => $el.focus())"
            class="w-full px-3 py-2 border border-gray-200 rounded text-sm focus:outline-none focus:ring-2 focus:ring-brand-blue/40 focus:border-brand-blue"
            autocomplete="off"
            required
          />
```

- [ ] **Step 2: Add a global Esc keybinding that clears any open confirm-state**

In `src/lib/agent/ui/index.html`, add on the `<body>` tag:

```html
@keydown.escape.window="onEscape()"
```

In `src/lib/agent/ui/app.js`, add an `onEscape` method:

```js
    onEscape() {
      // If the user is on the logs view, toggle auto-refresh off (quick pause).
      if (this.route.view === "apps" && this.route.subtab === "logs" && this.appLogsAuto) {
        this.appLogsAuto = false
        this.stopLogsPoll()
      }
    },
```

- [ ] **Step 3: Polish Playwright test — favicon and focus**

Create `tests/playwright/polish.spec.ts`:

```ts
import { test, expect } from "@playwright/test"
import { startTestServer, type TestServerHandle } from "./helpers/server"

let srv: TestServerHandle

test.beforeAll(async () => { srv = await startTestServer({ apiKey: "right-key" }) })
test.afterAll(() => srv.cleanup())

test("favicon link is present", async ({ page }) => {
  await page.goto(`${srv.url}/ui`)
  const href = await page.locator('link[rel="icon"]').getAttribute("href")
  expect(href).toMatch(/^data:image\/svg\+xml/)
})

test("login input is focused on load", async ({ page }) => {
  await page.goto(`${srv.url}/ui`)
  await expect(page.locator("#apiKeyInput")).toBeFocused()
})

test("Esc on logs pane stops auto-refresh", async ({ page, context }) => {
  // Seed an app and open logs
  await fetch(`${srv.url}/apps`, {
    method: "POST",
    headers: { "X-API-Key": "right-key", "Content-Type": "application/json" },
    body: JSON.stringify({ name: "escapp", image: "nginx:alpine", internalPort: 80 }),
  })
  await context.addInitScript(() => sessionStorage.setItem("siteio_api_key", "right-key"))
  await page.goto(`${srv.url}/ui#/apps/escapp/logs`)
  await expect(page.locator('input[type="checkbox"]')).toBeChecked()
  await page.keyboard.press("Escape")
  await expect(page.locator('input[type="checkbox"]')).not.toBeChecked()
})
```

- [ ] **Step 4: Full regression run**

Run: `bun test && bun run typecheck && bun run test:e2e`
Expected: all pass.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agent/ui/ tests/playwright/polish.spec.ts
git commit -m "$(cat <<'EOF'
feat(admin-ui): polish pass — favicon, login focus, Esc pauses logs

Task 9: inline SVG favicon, autofocus the API key input on login, and
bind Esc on the logs pane to pause auto-refresh as a quick hotkey.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Done criteria

- All tasks committed.
- `bun test && bun run typecheck && bun run test:e2e` passes on a clean checkout.
- The shipped binary (`bun build --compile src/cli.ts`) includes the three UI asset strings (Bun bakes text imports into `--compile` output automatically).
- Hitting `https://api.<your-domain>/ui` with a valid API key shows apps, sites, and groups, and all lifecycle + rollback actions work end-to-end against a real agent.
